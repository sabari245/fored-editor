import { Dispatch } from "react"

interface setContentProps {
    setState: Dispatch<React.SetStateAction<string>>;
    node: HTMLElement | null;
}
export function setContent(props: setContentProps){
    
}